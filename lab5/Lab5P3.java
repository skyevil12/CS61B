public class Lab5P3 {
	//part a, compile success even if the constant value if different
	//part b, compile fail even if change two constant as the same value
	//part c, set static access super class could get the parent constant value
}